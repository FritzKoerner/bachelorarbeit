---
phase: 01-scaffolding-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/training_config.yaml
  - controllers/__init__.py
  - controllers/velocity_controller.py
  - envs/__init__.py
  - requirements.txt
  - assets/robots  # symlink
autonomous: true

must_haves:
  truths:
    - "Project directories exist: config/, controllers/, envs/, assets/robots/, runs/"
    - "assets/robots symlinks to prototyp_2 assets and draugas_genesis.urdf is reachable"
    - "VelocityController.compute_rpm() accepts (n_envs,4) action tensors and returns (n_envs,4) RPMs"
    - "config/training_config.yaml is loadable with yaml.safe_load() and contains env/spawn/controller sections"
  artifacts:
    - path: "config/training_config.yaml"
      provides: "All Phase 1 hyperparameters"
      contains: "n_envs: 16"
    - path: "controllers/velocity_controller.py"
      provides: "VelocityController class with compute_rpm()"
      exports: ["VelocityController"]
    - path: "controllers/__init__.py"
      provides: "Package init"
    - path: "envs/__init__.py"
      provides: "Package init"
    - path: "requirements.txt"
      provides: "Pinned dependencies"
  key_links:
    - from: "controllers/velocity_controller.py"
      to: "prototyp_2/controllers/low_level_controller.py"
      via: "verbatim port"
      pattern: "class VelocityController"
    - from: "assets/robots"
      to: "/home/fritz-sfl/Bachelorarbeit/genesis/prototyp_2/assets/robots"
      via: "symlink"
      pattern: "draugas_genesis.urdf"
---

<objective>
Create the project scaffold (directories, packages, symlinked assets, config YAML) and port the VelocityController from prototyp_2. No Genesis imports or simulation code yet.

Purpose: Establish the foundation that CoordinateLandingEnv (Plan 02) will build on. Having the controller and config ready lets Plan 02 focus entirely on the env logic.
Output: Runnable Python packages, config file, and controller ready for import.
</objective>

<execution_context>
@/home/fritz-sfl/.claude/get-shit-done/workflows/execute-plan.md
@/home/fritz-sfl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-environment/01-RESEARCH.md

# Reference source for verbatim port
@/home/fritz-sfl/Bachelorarbeit/genesis/prototyp_2/controllers/low_level_controller.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project directory structure and symlink assets</name>
  <files>
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/envs/__init__.py
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/controllers/__init__.py
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/assets/robots
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/runs/.gitkeep
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/requirements.txt
  </files>
  <action>
    Run from project root /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/:

    1. Create package directories and empty __init__.py files:
       - mkdir -p envs controllers runs
       - touch envs/__init__.py controllers/__init__.py runs/.gitkeep

    2. Create assets symlink pointing to prototyp_2's assets/robots directory:
       - mkdir -p assets
       - ln -s /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_2/assets/robots assets/robots
       (Use absolute path for the symlink target to avoid working-directory fragility.)

    3. Create requirements.txt with pinned versions:
       ```
       genesis-world==0.3.13
       torch>=2.10.0
       numpy>=2.3.0
       PyYAML>=6.0
       stable-baselines3==2.7.1
       gymnasium==1.2.3
       ```
       (Do not install now; Phase 3 handles training setup. Just document deps.)

    Verify that `assets/robots/draugas/draugas_genesis.urdf` is reachable through the symlink:
       ls assets/robots/draugas/draugas_genesis.urdf
  </action>
  <verify>
    Run from project root:
    ls envs/__init__.py controllers/__init__.py runs/.gitkeep requirements.txt
    ls -la assets/robots  # should show symlink -> /home/fritz-sfl/.../prototyp_2/assets/robots
    ls assets/robots/draugas/draugas_genesis.urdf  # must exist
  </verify>
  <done>
    All directories exist. assets/robots is a symlink. draugas_genesis.urdf is accessible at assets/robots/draugas/draugas_genesis.urdf. requirements.txt exists with all 6 dependencies listed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create training_config.yaml</name>
  <files>
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/config/training_config.yaml
  </files>
  <action>
    Create config/training_config.yaml with the following content (adapted from prototyp_2, updated for global coordinate project parameters):

    ```yaml
    env:
      n_envs: 16
      max_episode_steps: 500

    scene:
      show_viewer: false
      env_spacing: 80.0  # 80m x 80m per env to fit ±30m target range

    spawn:
      height_min: 10.0        # drone spawn altitude minimum (m) — differs from prototyp_2 (was 6.0)
      height_max: 20.0        # drone spawn altitude maximum (m) — differs from prototyp_2 (was 10.0)
      target_range: 30.0      # target x/y sampled in [-30, +30] m
      drone_offset_range: 10.0  # drone x/y offset from target: ±10m

    controller:
      base_hover_rpm: 1789.2  # URDF-derived: sqrt(0.714 * 9.81 / (4 * 5.47e-07))
      max_rpm: 25000
      kp_vel: 5.0
      kd_vel: 1.0
      kp_att: 10.0
      mass: 0.714
      gravity: 9.81
      vel_scale: 2.0          # action [-1,1] -> velocity m/s scaling
      yaw_scale: 1.0          # action [-1,1] -> yaw rate rad/s scaling

    # reward: {}  # populated in Phase 2
    # ppo: {}     # populated in Phase 3
    ```

    Create the directory first: mkdir -p config
  </action>
  <verify>
    python3 -c "import yaml; c = yaml.safe_load(open('config/training_config.yaml')); assert c['env']['n_envs'] == 16; assert c['spawn']['target_range'] == 30.0; assert c['controller']['base_hover_rpm'] == 1789.2; print('Config OK')"
    (Run from project root /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/)
  </verify>
  <done>
    config/training_config.yaml loads without errors. All required sections (env, scene, spawn, controller) are present with correct values. n_envs=16, height_min=10.0, height_max=20.0, target_range=30.0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Port VelocityController from prototyp_2</name>
  <files>
    /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/controllers/velocity_controller.py
  </files>
  <action>
    Copy controllers/velocity_controller.py from prototyp_2's controllers/low_level_controller.py VERBATIM — same class name VelocityController, same logic, same constants. Do not rename or refactor. The only change allowed is the filename (low_level_controller.py -> velocity_controller.py) and updating the module docstring to reference this project.

    Source to copy: /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_2/controllers/low_level_controller.py

    The file contains:
    - Class VelocityController with __init__, reset(), compute_rpm(), _quat_to_rp()
    - URDF-derived constants: base_hover_rpm=1789.2, max_rpm=25000
    - PD control hierarchy: velocity error -> acceleration -> thrust + attitude -> RPM mix
    - Motor order: FR, FL, BL, BR (X configuration)
    - Propeller spins: [1, -1, 1, -1]

    After copying, update controllers/__init__.py to export VelocityController:
    ```python
    from controllers.velocity_controller import VelocityController

    __all__ = ["VelocityController"]
    ```
  </action>
  <verify>
    Run from project root:
    python3 -c "
    import torch
    from controllers.velocity_controller import VelocityController
    ctrl = VelocityController(device=torch.device('cpu'))
    n = 4
    rpms = ctrl.compute_rpm(
        target_vel=torch.zeros(n, 3),
        target_yaw_rate=torch.zeros(n),
        current_vel=torch.zeros(n, 3),
        current_quat=torch.tensor([[1,0,0,0]] * n, dtype=torch.float32),
    )
    assert rpms.shape == (n, 4), f'Bad shape: {rpms.shape}'
    assert (rpms >= 0).all(), 'Negative RPMs'
    assert (rpms <= 25000).all(), 'RPMs exceed max'
    print(f'VelocityController OK — hover RPMs: {rpms[0].tolist()}')
    "
  </verify>
  <done>
    VelocityController imports cleanly. compute_rpm() with zero inputs returns (4,4) tensor of non-negative RPMs near base_hover_rpm (≈1789.2). No AttributeError or import failures.
  </done>
</task>

</tasks>

<verification>
From project root /home/fritz-sfl/Bachelorarbeit/genesis/prototyp_global_coordinate/:

1. Directory structure: ls -la envs/ controllers/ config/ assets/ runs/
2. Asset symlink resolves: ls assets/robots/draugas/draugas_genesis.urdf
3. Config loads: python3 -c "import yaml; print(yaml.safe_load(open('config/training_config.yaml'))['env'])"
4. Controller imports and runs: python3 -c "from controllers.velocity_controller import VelocityController; print('OK')"
</verification>

<success_criteria>
- All directories created (envs/, controllers/, config/, assets/, runs/)
- assets/robots symlinks to prototyp_2's assets/robots (draugas_genesis.urdf reachable)
- training_config.yaml has correct values: n_envs=16, height_min=10.0, height_max=20.0, target_range=30.0, env_spacing=80.0
- VelocityController.compute_rpm() returns (n_envs, 4) RPMs in [0, 25000] range
</success_criteria>

<output>
After completion, create .planning/phases/01-scaffolding-environment/01-01-SUMMARY.md
</output>
